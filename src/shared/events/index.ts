// Placeholder for the `shared/events` layer (Structural Seed, ARCHITECTURE.md).
//
// Will hold the abstract `IntegrationEvent<T>` base class and its concrete subclasses
// (`WagerTransactionProcessed`, `WagerTransactionRejected`, `WalletBalanceChanged`,
// `WagerTransactionPendingReference` — README section 11). Introduced starting Epic 3
// (transactional outbox) once there is a real event to publish.
export {};
