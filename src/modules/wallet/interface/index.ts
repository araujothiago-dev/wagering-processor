// Placeholder for `modules/wallet/interface` (Structural Seed, ARCHITECTURE.md).
//
// Will hold `WalletController` (POST /wallets, GET /wallets/:id, GET /wallets/:id/ledger) and
// its request/response DTOs. Introduced starting Story 1.2.
//
// Rule (AD-2): this layer calls only `application` use cases — never `infrastructure`
// repositories directly.
export {};
