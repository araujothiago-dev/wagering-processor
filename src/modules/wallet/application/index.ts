// Placeholder for `modules/wallet/application` (Structural Seed, ARCHITECTURE.md).
//
// Will hold use cases (`OpenWallet`, `GetWallet`, `GetLedger`, `ReconcileWallet`, ...) and the
// ports they depend on (`WalletRepository`, `Clock`, ...). Introduced starting Story 1.2.
//
// Rule (AD-2): this layer only imports `domain` plus the ports it declares itself — never
// NestJS, TypeORM, or any concrete infrastructure adapter.
export {};
